# sql/ddl/

## 목적
Snowflake DDL(Data Definition Language) 스크립트를 관리한다.

## 구조
- `NNN_<설명>.sql` — 번호 prefix로 실행 순서 보장
- 모든 DDL은 `IF NOT EXISTS`로 멱등성 보장

## 현재 파일
| 파일 | 이슈 | 설명 |
|------|------|------|
| `001_create_database_schema.sql` | #16 | MOVING_INTEL DB + ANALYTICS 스키마 생성 |
