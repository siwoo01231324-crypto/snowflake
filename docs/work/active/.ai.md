# docs/work/active/

## 목적
현재 진행 중인 이슈의 작업 내역 폴더. `/start-issue` 실행 시 자동 생성된다.

## 폴더 구조
```
active/
└── {PADDED_NUM}-{이슈명}/   예: 000001-initial-setup/
    └── work.md              작업 내역·진행 상황·메모
```

## 규칙
- 폴더는 `/si` 커맨드가 자동 생성
- 작업 완료 후 `/fi` 실행 시 `done/`으로 이동
