# 구현 플랜 — 초기 프로젝트 셋업

## AC 체크리스트
- [ ] `.gitignore`에 `.omc/` 추가
- [ ] `.worktree/` 폴더 생성 + `.gitignore`에 등록
- [ ] `docs/whiteboard/` 폴더 생성 및 배경 파일 추가

## 작업 순서
1. `.gitignore` 업데이트 (`.omc/`, `.worktree/`)
2. `.worktree/.gitkeep` 확인 (이미 생성됨)
3. `docs/whiteboard/` 폴더 생성 및 배경 파일 배치
4. `.ai.md` 최신화
